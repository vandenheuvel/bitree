[package]
name = "bitree-benchmark"
version = "0.0.0"
edition = "2024"
publish = false

[[bin]]
name = "benchmark"
path = "main.rs"

[dependencies]
bitree = { path = "../.." }
fenwick = "2.0"
ftree = "1.3"
segment-tree = "2.0"

[profile.release]
lto = "fat"
codegen-units = 1
